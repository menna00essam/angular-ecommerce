export interface HeroSlide {
  id: number;
  title: string;
  description: string;
  buttonText: string;
  imageUrl: string;
  loading: boolean;
}

export interface Feature {
  icon: string;
  title: string;
  description: string;
}